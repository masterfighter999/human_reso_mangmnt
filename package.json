{
  "name": "human-resource-management-system",
  "version": "1.0.0",
  "description": "HRMS with React, Vite, Node, Express, GraphQL and PostgreSQL",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev:js --prefix backend",
    "dev:ts-backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:ts-backend\" \"npm run dev:frontend\"",
    "db:migrate": "node backend/migrate.js"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
